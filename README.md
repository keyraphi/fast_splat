# Fast Splat 2D

A simple and fast function for splatting a batch of small images into a target image. It is implemented in C++ and CUDA and bound to Python for easy use from PyTorch.

## Installation

```bash
# Install dependencies
chmod +x install_dependencies.sh
./install_dependencies.sh


# Build and install
pip install .
```

## Usage

